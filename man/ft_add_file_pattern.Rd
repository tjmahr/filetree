% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/filetree.R
\name{ft_add_file_pattern}
\alias{ft_add_file_pattern}
\title{Register file-name patterns for a layer}
\usage{
ft_add_file_pattern(ft, layer, patterns, when = NULL, with = NULL)
}
\arguments{
\item{ft}{A \code{filetree} object.}

\item{layer}{Layer at which the files live (must be listed in \code{ft$layers}).}

\item{patterns}{Named character vector of file-name patterns that may use
\code{{placeholder}} references tied to \code{ft}'s regex pool.}

\item{when}{Optional named character vector or named list of exact-match
conditions. A conditional file pattern is applied only when every condition
matches an extracted field or raw layer value with the same name. Use a
list when a condition can match any of several values.}

\item{with}{Optional named character vector of pattern-local regex
definitions. These definitions override \code{ft}'s regex pool for this file
pattern only.}
}
\value{
The updated \code{filetree} object.
}
\description{
Compile named patterns that validate and extract captures from file names
for files that belong to a specific layer.
}
\examples{
root <- system.file("demo-1", package = "filetree")

ft <- ft_init(root, c("subject", "time", "data")) |>
  ft_add_regex(c(
    subject = "\\\\w{2}-\\\\d{2}",
    time = "day\\\\d{2}",
    task = "red|green"
  )) |>
  ft_add_file_pattern(
    "data",
    c(txt = "{subject}_{task}.txt"),
    when = list(time = c("day01", "day02"))
  ) |>
  ft_add_file_pattern(
    "data",
    c(wav = "{subject}_{task}.wav"),
    when = c(time = "day03"),
    with = c(task = "yellow")
  )

ft
}
